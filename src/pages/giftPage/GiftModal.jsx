import { View } from "@tarojs/components"
import { memo } from "react"

const GiftModal = () => {
    return (
        <View>
            <View>modal</View>
        </View>
    )
}

export default memo(GiftModal);